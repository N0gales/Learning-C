#include <stdio.h>

int main() {
    int age = 65;
    int isMember = 0;
	int isEligible = 1; // Define the variables here
    
    
    // Create the boolean expression here
 
    // Print the result
    printf("Eligible for discount? %d\n", isEligible);
    
    return 0;
}
