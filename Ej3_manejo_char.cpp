#include <stdio.h> //Header y llamada a librería

int main() { //Llamada a main es decir la ejecucion
    char symbol; // Definicion de variable
    symbol = '@'; // Asignacion de valor a la variable al ser simbolo siempre con ''
    printf("The symbol is: %c", symbol); 
    // \" \" = Inserta comillado interno
    return 0;
}

